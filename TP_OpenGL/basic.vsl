#version 430

layout (location=12) in vec4 pos;
layout (location=13) in vec3 normal;

out vec3 normal_out;

uniform mat4 model;
uniform mat4 view;
uniform mat4 projection;

void main()
{
	 gl_Position = projection * view * model * pos;
	 //normal_out = normal;
}
